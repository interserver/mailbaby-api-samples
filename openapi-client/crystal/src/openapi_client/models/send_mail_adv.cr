# #MailBaby Email Delivery and Management Service API
#
##**Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.** # Overview This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net). # Authentication In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site. We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#
#The version of the OpenAPI document: 1.3.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.19.0
#

module OpenAPIClient
  # Details for an Email
  class SendMailAdv
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The subject or title of the email
    @[JSON::Field(key: "subject", type: String, nillable: false, emit_null: false)]
    property subject : String

    # The main email contents.
    @[JSON::Field(key: "body", type: String, nillable: false, emit_null: false)]
    property body : String

    @[JSON::Field(key: "from", type: EmailAddressTypes, nillable: false, emit_null: false)]
    property from : EmailAddressTypes

    @[JSON::Field(key: "to", type: EmailAddressesTypes, nillable: false, emit_null: false)]
    property to : EmailAddressesTypes

    # Optional properties
    @[JSON::Field(key: "replyto", type: EmailAddressesTypes?, nillable: true, emit_null: false)]
    property replyto : EmailAddressesTypes?

    @[JSON::Field(key: "cc", type: EmailAddressesTypes?, nillable: true, emit_null: false)]
    property cc : EmailAddressesTypes?

    @[JSON::Field(key: "bcc", type: EmailAddressesTypes?, nillable: true, emit_null: false)]
    property bcc : EmailAddressesTypes?

    # (optional) File attachments to include in the email.  The file contents must be base64 encoded!
    @[JSON::Field(key: "attachments", type: Array(MailAttachment)?, nillable: true, emit_null: false)]
    property attachments : Array(MailAttachment)?

    # (optional)  ID of the Mail order within our system to use as the Mail Account.
    @[JSON::Field(key: "id", type: Int64?, nillable: true, emit_null: false)]
    property id : Int64?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@subject : String, @body : String, @from : EmailAddressTypes, @to : EmailAddressesTypes, @replyto : EmailAddressesTypes? = nil, @cc : EmailAddressesTypes? = nil, @bcc : EmailAddressesTypes? = nil, @attachments : Array(MailAttachment)? = nil, @id : Int64? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          subject == other.subject &&
          body == other.body &&
          from == other.from &&
          to == other.to &&
          replyto == other.replyto &&
          cc == other.cc &&
          bcc == other.bcc &&
          attachments == other.attachments &&
          id == other.id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [subject, body, from, to, replyto, cc, bcc, attachments, id].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["subject"] = _to_h(subject)
      hash["body"] = _to_h(body)
      hash["from"] = _to_h(from)
      hash["to"] = _to_h(to)
      hash["replyto"] = _to_h(replyto)
      hash["cc"] = _to_h(cc)
      hash["bcc"] = _to_h(bcc)
      hash["attachments"] = _to_h(attachments)
      hash["id"] = _to_h(id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
