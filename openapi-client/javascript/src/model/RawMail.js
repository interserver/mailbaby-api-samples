/**
 * MailBaby Email Delivery and Management Service API
 * **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.** # Overview This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net). # Authentication In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site. We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
 *
 * The version of the OpenAPI document: 1.3.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RawMail model module.
 * @module model/RawMail
 * @version 1.3.0
 */
class RawMail {
    /**
     * Constructs a new <code>RawMail</code>.
     * Raw Email Object
     * @alias module:model/RawMail
     * @param rawEmail {String} The entire email contents
     */
    constructor(rawEmail) { 
        
        RawMail.initialize(this, rawEmail);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, rawEmail) { 
        obj['raw_email'] = rawEmail;
    }

    /**
     * Constructs a <code>RawMail</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RawMail} obj Optional instance to populate.
     * @return {module:model/RawMail} The populated <code>RawMail</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RawMail();

            if (data.hasOwnProperty('raw_email')) {
                obj['raw_email'] = ApiClient.convertToType(data['raw_email'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RawMail</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RawMail</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RawMail.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['raw_email'] && !(typeof data['raw_email'] === 'string' || data['raw_email'] instanceof String)) {
            throw new Error("Expected the field `raw_email` to be a primitive type in the JSON string but got " + data['raw_email']);
        }

        return true;
    }


}

RawMail.RequiredProperties = ["raw_email"];

/**
 * The entire email contents
 * @member {String} raw_email
 */
RawMail.prototype['raw_email'] = undefined;






export default RawMail;

