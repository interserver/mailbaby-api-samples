(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Send_mail_adv.t : Request body for `POST /mail/advsend`.  Provides full control over all email headers and supports multiple recipients, CC, BCC, Reply-To, and file attachments.  Address fields (`from`, `to`, `replyto`, `cc`, `bcc`) each accept either a plain RFC 822 string (e.g. `\''Joe <joe@example.com>\''` or a comma-separated list) or a structured array of `{\''email\'': \''...\'', \''name\'': \''...\''}` objects.  HTML detection is automatic based on whether `body` contains HTML tags.
 *)



    
        type t = {
                              subject: string
                  
                  
                
                
                ; [@key "subject"]
                    (** The subject line of the email. *)
                                      body: string
                  
                  
                
                
                ; [@key "body"]
                    (** The email body.  If the string contains any HTML tags the message is automatically sent as `text/html`; otherwise it is sent as `text/plain`. *)
                                      from: Email_address_types.t
                  
                  
                
                
                ; [@key "from"]
                                              _to: Email_addresses_types.t
                  
                  
                
                
                ; [@key "to"]
                                              replyto: Email_addresses_types.t
                  
                   option [@default None]
                
                
                ; [@key "replyto"]
                                              cc: Email_addresses_types.t
                  
                   option [@default None]
                
                
                ; [@key "cc"]
                                              bcc: Email_addresses_types.t
                  
                   option [@default None]
                
                
                ; [@key "bcc"]
                                              attachments: Mail_attachment.t list
                 [@default []]
                
                ; [@key "attachments"]
                    (** Optional list of file attachments.  Each file must be base64-encoded. Include `filename` so recipients see a meaningful attachment name. *)
                                      id: int64
                  
                   option [@default None]
                
                
                ; [@key "id"]
                    (** Optional numeric ID of the mail order to send through.  If omitted the first active order on your account is used automatically.  Valid IDs are returned by `GET /mail`. *)
                } [@@deriving yojson { strict = false }, show, eq ];;
        
        (** Request body for `POST /mail/advsend`.  Provides full control over all email headers and supports multiple recipients, CC, BCC, Reply-To, and file attachments.  Address fields (`from`, `to`, `replyto`, `cc`, `bcc`) each accept either a plain RFC 822 string (e.g. `\''Joe <joe@example.com>\''` or a comma-separated list) or a structured array of `{\''email\'': \''...\'', \''name\'': \''...\''}` objects.  HTML detection is automatic based on whether `body` contains HTML tags. *)
        let create (subject : string) (body : string) (from : Email_address_types.t) (_to : Email_addresses_types.t) : t = {
            subject = subject;
            body = body;
            from = from;
            _to = _to;
            replyto = None;
            cc = None;
            bcc = None;
            attachments = [];
            id = None;
        }
    

