# MailBaby Email Delivery API
#
# **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**   # 📌 Overview  This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).   # 🔐 Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site.  We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#
# OpenAPI spec version: 1.0.1
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' SendMailAdvAttachments Class
#'
#' @field filename 
#' @field data 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SendMailAdvAttachments <- R6::R6Class(
  'SendMailAdvAttachments',
  public = list(
    `filename` = NULL,
    `data` = NULL,
    initialize = function(`filename`, `data`){
      if (!missing(`filename`)) {
        stopifnot(is.character(`filename`), length(`filename`) == 1)
        self$`filename` <- `filename`
      }
      if (!missing(`data`)) {
        stopifnot(is.character(`data`), length(`data`) == 1)
        self$`data` <- `data`
      }
    },
    toJSON = function() {
      SendMailAdvAttachmentsObject <- list()
      if (!is.null(self$`filename`)) {
        SendMailAdvAttachmentsObject[['filename']] <- self$`filename`
      }
      if (!is.null(self$`data`)) {
        SendMailAdvAttachmentsObject[['data']] <- self$`data`
      }

      SendMailAdvAttachmentsObject
    },
    fromJSON = function(SendMailAdvAttachmentsJson) {
      SendMailAdvAttachmentsObject <- jsonlite::fromJSON(SendMailAdvAttachmentsJson)
      if (!is.null(SendMailAdvAttachmentsObject$`filename`)) {
        self$`filename` <- SendMailAdvAttachmentsObject$`filename`
      }
      if (!is.null(SendMailAdvAttachmentsObject$`data`)) {
        self$`data` <- SendMailAdvAttachmentsObject$`data`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "filename": %s,
           "data": %s
        }',
        self$`filename`,
        self$`data`
      )
    },
    fromJSONString = function(SendMailAdvAttachmentsJson) {
      SendMailAdvAttachmentsObject <- jsonlite::fromJSON(SendMailAdvAttachmentsJson)
      self$`filename` <- SendMailAdvAttachmentsObject$`filename`
      self$`data` <- SendMailAdvAttachmentsObject$`data`
    }
  )
)
