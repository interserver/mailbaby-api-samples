# MailBaby Email Delivery and Management Service API
#
# **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.** # Overview This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net). # Authentication In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site. We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#
# OpenAPI spec version: 1.1.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' ErrorMessage Class
#'
#' @field code 
#' @field message 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ErrorMessage <- R6::R6Class(
  'ErrorMessage',
  public = list(
    `code` = NULL,
    `message` = NULL,
    initialize = function(`code`, `message`){
      if (!missing(`code`)) {
        stopifnot(is.numeric(`code`), length(`code`) == 1)
        self$`code` <- `code`
      }
      if (!missing(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
        self$`message` <- `message`
      }
    },
    toJSON = function() {
      ErrorMessageObject <- list()
      if (!is.null(self$`code`)) {
        ErrorMessageObject[['code']] <- self$`code`
      }
      if (!is.null(self$`message`)) {
        ErrorMessageObject[['message']] <- self$`message`
      }

      ErrorMessageObject
    },
    fromJSON = function(ErrorMessageJson) {
      ErrorMessageObject <- jsonlite::fromJSON(ErrorMessageJson)
      if (!is.null(ErrorMessageObject$`code`)) {
        self$`code` <- ErrorMessageObject$`code`
      }
      if (!is.null(ErrorMessageObject$`message`)) {
        self$`message` <- ErrorMessageObject$`message`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "code": %d,
           "message": %s
        }',
        self$`code`,
        self$`message`
      )
    },
    fromJSONString = function(ErrorMessageJson) {
      ErrorMessageObject <- jsonlite::fromJSON(ErrorMessageJson)
      self$`code` <- ErrorMessageObject$`code`
      self$`message` <- ErrorMessageObject$`message`
    }
  )
)
