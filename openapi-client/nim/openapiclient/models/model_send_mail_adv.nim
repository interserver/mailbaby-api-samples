#
# MailBaby Email Delivery API
# 
# **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**   # 📌 Overview  This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).   # 🔐 Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site.    We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
# The version of the OpenAPI document: 1.0.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables

import model_send_mail_adv_attachments_inner
import model_send_mail_adv_bcc_inner
import model_send_mail_adv_cc_inner
import model_send_mail_adv_from
import model_send_mail_adv_replyto_inner
import model_send_mail_adv_to_inner

type SendMailAdv* = object
  ## Details for an Email
  subject*: string ## The subject or title of the email
  body*: string ## The main email contents.
  `from`*: SendMailAdv_from
  to*: seq[SendMailAdv_to_inner] ## A list of destionation email addresses to send this to
  replyto*: seq[SendMailAdv_replyto_inner] ## (optional) A list of email addresses that specify where replies to the email should be sent instead of the _from_ address.
  cc*: seq[SendMailAdv_cc_inner] ## (optional) A list of email addresses to carbon copy this message to.  They are listed on the email and anyone getting the email can see this full list of Contacts who received the email as well.
  bcc*: seq[SendMailAdv_bcc_inner] ## (optional) list of email addresses that should receive copies of the email.  They are hidden on the email and anyone gettitng the email would not see the other people getting the email in this list.
  attachments*: seq[SendMailAdv_attachments_inner] ## (optional) File attachments to include in the email.  The file contents must be base64 encoded!
  id*: int64 ## (optional)  ID of the Mail order within our system to use as the Mail Account.
