#' MailBaby Email Delivery API
#'
#' **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**   # 📌 Overview  This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).   # 🔐 Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site.    We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#'
#' The version of the OpenAPI document: 1.0.0
#' Contact: support@interserver.net
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title Default operations
#' @description openapi.Default
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetMailOrders } \emph{ displays a list of mail service orders }
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{getMailOrders_200_response_inner} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[GetMailOrders200ResponseInner] 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Unauthorized
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PingServer } \emph{ Checks if the server is running }
#'
#' \itemize{
#'
#'
#' \item status code : 200 | Server is up and running
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Something is wrong
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SendAdvMail } \emph{ Sends an Email with Advanced Options }
#' Sends An email through one of your mail orders allowing additional options such as file attachments, cc, bcc, etc.
#'
#' \itemize{
#' \item \emph{ @param } send_mail_adv \link{SendMailAdv}
#' \item \emph{ @returnType } \link{GenericResponse} \cr
#'
#'
#' \item status code : 200 | search results matching criteria
#'
#' \item return type : GenericResponse 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The specified resource was not found
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SendMail } \emph{ Sends an Email }
#' Sends an email through one of your mail orders.  *Note*: If you want to send to multiple recipients or use file attachments use the advsend (Advanced Send) call instead. 
#'
#' \itemize{
#' \item \emph{ @param } to character
#' \item \emph{ @param } from character
#' \item \emph{ @param } subject character
#' \item \emph{ @param } body character
#' \item \emph{ @returnType } \link{GenericResponse} \cr
#'
#'
#' \item status code : 200 | search results matching criteria
#'
#' \item return type : GenericResponse 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The specified resource was not found
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Unauthorized
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The specified resource was not found
#'
#' \item return type : GetMailOrders401Response 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ViewMailLog } \emph{ displays the mail log }
#' By passing in the appropriate options, you can search for available inventory in the system 
#'
#' \itemize{
#' \item \emph{ @param } id integer
#' \item \emph{ @param } search character
#' \item \emph{ @param } skip integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @returnType } \link{MailLog} \cr
#'
#'
#' \item status code : 200 | search results matching criteria
#'
#' \item return type : MailLog 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | bad input parameter
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetMailOrders  ####################
#'
#' library(openapi)
#'
#' #displays a list of mail service orders
#' api.instance <- DefaultApi$new()
#'
#' #Configure API key authorization: apiKeyAuth
#' api.instance$api_client$api_keys['X-API-KEY'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$GetMailOrders()
#'
#'
#' ####################  PingServer  ####################
#'
#' library(openapi)
#'
#' #Checks if the server is running
#' api.instance <- DefaultApi$new()
#'
#' result <- api.instance$PingServer()
#'
#'
#' ####################  SendAdvMail  ####################
#'
#' library(openapi)
#' var.send_mail_adv <- SendMailAdv$new() # SendMailAdv | 
#'
#' #Sends an Email with Advanced Options
#' api.instance <- DefaultApi$new()
#'
#' #Configure API key authorization: apiKeyAuth
#' api.instance$api_client$api_keys['X-API-KEY'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SendAdvMail(var.send_mail_adv)
#'
#'
#' ####################  SendMail  ####################
#'
#' library(openapi)
#' var.to <- "to_example" # character | The Contact whom is the primary recipient of this email.
#' var.from <- "from_example" # character | The contact whom is the this email is from.
#' var.subject <- "subject_example" # character | The subject or title of the email
#' var.body <- "body_example" # character | The main email contents.
#'
#' #Sends an Email
#' api.instance <- DefaultApi$new()
#'
#' #Configure API key authorization: apiKeyAuth
#' api.instance$api_client$api_keys['X-API-KEY'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SendMail(var.to, var.from, var.subject, var.body)
#'
#'
#' ####################  ViewMailLog  ####################
#'
#' library(openapi)
#' var.id <- 56 # integer | The ID of your mail order this will be sent through.
#' var.search <- "search_example" # character | pass an optional search string for looking up inventory
#' var.skip <- 0 # integer | number of records to skip for pagination
#' var.limit <- 100 # integer | maximum number of records to return
#'
#' #displays the mail log
#' api.instance <- DefaultApi$new()
#'
#' #Configure API key authorization: apiKeyAuth
#' api.instance$api_client$api_keys['X-API-KEY'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$ViewMailLog(id=var.id, search=var.search, skip=var.skip, limit=var.limit)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DefaultApi <- R6::R6Class(
  "DefaultApi",
  public = list(
    api_client = NULL,
    #' Initialize a new DefaultApi.
    #'
    #' @description
    #' Initialize a new DefaultApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' displays a list of mail service orders
    #'
    #' @description
    #' displays a list of mail service orders
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[GetMailOrders200ResponseInner]
    #' @export
    GetMailOrders = function(data_file = NULL, ...) {
      api_response <- self$GetMailOrdersWithHttpInfo(data_file = data_file, ...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' displays a list of mail service orders
    #'
    #' @description
    #' displays a list of mail service orders
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[GetMailOrders200ResponseInner]) with additional information such as HTTP status code, headers
    #' @export
    GetMailOrdersWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      body <- NULL
      url_path <- "/mail"
      # API key authentication
      if ("X-API-KEY" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["X-API-KEY"]) > 0) {
        header_params["X-API-KEY"] <- paste(unlist(self$api_client$api_keys["X-API-KEY"]), collapse = "")
      }

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(httr::content(resp, "text", encoding = "UTF-8", simplifyVector = FALSE), data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(resp, "array[GetMailOrders200ResponseInner]", loadNamespace("openapi")),
          error = function(e) {
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserialized_resp_obj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    #' Checks if the server is running
    #'
    #' @description
    #' Checks if the server is running
    #'
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PingServer = function(...) {
      api_response <- self$PingServerWithHttpInfo(...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' Checks if the server is running
    #'
    #' @description
    #' Checks if the server is running
    #'
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PingServerWithHttpInfo = function(...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      body <- NULL
      url_path <- "/ping"

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    #' Sends an Email with Advanced Options
    #'
    #' @description
    #' Sends an Email with Advanced Options
    #'
    #' @param send_mail_adv 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GenericResponse
    #' @export
    SendAdvMail = function(send_mail_adv, data_file = NULL, ...) {
      api_response <- self$SendAdvMailWithHttpInfo(send_mail_adv, data_file = data_file, ...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' Sends an Email with Advanced Options
    #'
    #' @description
    #' Sends an Email with Advanced Options
    #'
    #' @param send_mail_adv 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GenericResponse) with additional information such as HTTP status code, headers
    #' @export
    SendAdvMailWithHttpInfo = function(send_mail_adv, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      if (missing(`send_mail_adv`)) {
        stop("Missing required parameter `send_mail_adv`.")
      }

      if (!missing(`send_mail_adv`)) {
        body <- `send_mail_adv`$toJSONString()
      } else {
        body <- NULL
      }

      url_path <- "/mail/advsend"
      # API key authentication
      if ("X-API-KEY" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["X-API-KEY"]) > 0) {
        header_params["X-API-KEY"] <- paste(unlist(self$api_client$api_keys["X-API-KEY"]), collapse = "")
      }

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(httr::content(resp, "text", encoding = "UTF-8", simplifyVector = FALSE), data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(resp, "GenericResponse", loadNamespace("openapi")),
          error = function(e) {
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserialized_resp_obj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    #' Sends an Email
    #'
    #' @description
    #' Sends an Email
    #'
    #' @param to The Contact whom is the primary recipient of this email.
    #' @param from The contact whom is the this email is from.
    #' @param subject The subject or title of the email
    #' @param body The main email contents.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GenericResponse
    #' @export
    SendMail = function(to, from, subject, body, data_file = NULL, ...) {
      api_response <- self$SendMailWithHttpInfo(to, from, subject, body, data_file = data_file, ...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' Sends an Email
    #'
    #' @description
    #' Sends an Email
    #'
    #' @param to The Contact whom is the primary recipient of this email.
    #' @param from The contact whom is the this email is from.
    #' @param subject The subject or title of the email
    #' @param body The main email contents.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GenericResponse) with additional information such as HTTP status code, headers
    #' @export
    SendMailWithHttpInfo = function(to, from, subject, body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      if (missing(`to`)) {
        stop("Missing required parameter `to`.")
      }

      if (missing(`from`)) {
        stop("Missing required parameter `from`.")
      }

      if (missing(`subject`)) {
        stop("Missing required parameter `subject`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      body <- list(
        "to" = to,
        "from" = from,
        "subject" = subject,
        "body" = body
      )

      url_path <- "/mail/send"
      # API key authentication
      if ("X-API-KEY" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["X-API-KEY"]) > 0) {
        header_params["X-API-KEY"] <- paste(unlist(self$api_client$api_keys["X-API-KEY"]), collapse = "")
      }

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(httr::content(resp, "text", encoding = "UTF-8", simplifyVector = FALSE), data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(resp, "GenericResponse", loadNamespace("openapi")),
          error = function(e) {
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserialized_resp_obj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    #' displays the mail log
    #'
    #' @description
    #' displays the mail log
    #'
    #' @param id (optional)The ID of your mail order this will be sent through. 
    #' @param search (optional)pass an optional search string for looking up inventory 
    #' @param skip (optional)number of records to skip for pagination  (default value: 0)
    #' @param limit (optional)maximum number of records to return  (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MailLog
    #' @export
    ViewMailLog = function(id=NULL, search=NULL, skip=0, limit=100, data_file = NULL, ...) {
      api_response <- self$ViewMailLogWithHttpInfo(id, search, skip, limit, data_file = data_file, ...)
      resp <- api_response$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        api_response$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        api_response
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        api_response
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        api_response
      }
    },
    #' displays the mail log
    #'
    #' @description
    #' displays the mail log
    #'
    #' @param id (optional) The ID of your mail order this will be sent through.
    #' @param search (optional) pass an optional search string for looking up inventory
    #' @param skip (optional) number of records to skip for pagination (default value: 0)
    #' @param limit (optional) maximum number of records to return (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MailLog) with additional information such as HTTP status code, headers
    #' @export
    ViewMailLogWithHttpInfo = function(id=NULL, search=NULL, skip=0, limit=100, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()

      body <- NULL
      url_path <- "/mail/log"
      # API key authentication
      if ("X-API-KEY" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["X-API-KEY"]) > 0) {
        header_params["X-API-KEY"] <- paste(unlist(self$api_client$api_keys["X-API-KEY"]), collapse = "")
      }

      resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
            write(httr::content(resp, "text", encoding = "UTF-8", simplifyVector = FALSE), data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(resp, "MailLog", loadNamespace("openapi")),
          error = function(e) {
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserialized_resp_obj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
