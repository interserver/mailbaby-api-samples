# #MailBaby Email Delivery and Management Service API
#
##**Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**  # Overview  This is the API interface to the [Mail Baby](https://mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).  # Mail Orders  Every sending account in MailBaby is backed by a **Mail Order** — a provisioned sending credential with a numeric `id` and a corresponding SMTP username (`mb<id>`).  Most calls accept an optional `id` parameter; when omitted the API automatically selects the first active order on your account. Use `GET /mail` to list all orders, and `GET /mail/{id}` to inspect a single order including its current SMTP password.  # Sending Email  Three sending methods are available depending on your use-case: | Endpoint | Best for | |----------|----------| | `POST /mail/send` | Simple single-recipient messages | | `POST /mail/advsend` | Multiple recipients, CC/BCC, attachments, named contacts | | `POST /mail/rawsend` | Pre-built RFC 822 messages (e.g. DKIM-signed payloads) |  After a successful send each endpoint returns a `GenericResponse` whose `text` field contains the **transaction ID** assigned by the relay.  This ID can later be matched against entries in `GET /mail/log` via the `mailid` query parameter.  # Filtering & Logs  `GET /mail/log` provides paginated access to every message accepted by the relay for your account.  Combine any of the query parameters to narrow results — e.g. `from`, `to`, `subject`, `messageId`, `origin`, `mx`, `startDate`/`endDate`, and `delivered`.  # Blocking  Two independent mechanisms exist for suppressing unwanted email: - **Block lists** (`GET /mail/blocks`, `POST /mail/blocks/delete`) — addresses flagged by the   system spam filters (LOCAL_BL_RCPT / MBTRAP rules in rspamd, and suspicious subjects). - **Deny rules** (`GET /mail/rules`, `POST /mail/rules`, `DELETE /mail/rules/{ruleId}`) —   custom rules you configure to reject specific senders, domains, destination addresses, or   subject-line prefixes before a message is even attempted.   # Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site. We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. Pass your key in the `X-API-KEY` HTTP request header for every protected call. 
#
#The version of the OpenAPI document: 1.4.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.20.0
#

require "uri"

module OpenAPIClient
  class HistoryApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Account usage statistics
    # Returns aggregate sending statistics for your mail account(s) across a selectable time window.  Useful for dashboards, billing reviews, and detecting unusual traffic patterns.  The response includes: - **`usage`** — total messages accepted by the relay during the current billing   cycle (used for cost calculation). - **`cost`** — estimated cost for the billing cycle based on the base plan price   plus per-email charges. - **`received`** / **`sent`** — count of messages accepted by the relay /   successfully delivered to the destination MX within the selected `time` window. - **`volume`** — top-500 breakdown of message counts grouped by source IP (`ip`),   destination address (`to`), and sender address (`from`) within the selected window.   **Time windows** (controlled by the `time` parameter): | Value | Window | |-------|--------| | `1h` | Last 1 hour (default) | | `24h` | Last 24 hours | | `7d` | Last 7 days | | `month` | Current calendar month (1st to now) | | `day` | Today (midnight to now) | | `billing` | Current billing cycle (last invoice date to next invoice date) | | `all` | All time | 
    # @return [MailStatsType]
    def get_stats(time : String? = nil)
      data, _status_code, _headers = get_stats_with_http_info(time)
      data
    end

    # Account usage statistics
    # Returns aggregate sending statistics for your mail account(s) across a selectable time window.  Useful for dashboards, billing reviews, and detecting unusual traffic patterns.  The response includes: - **&#x60;usage&#x60;** — total messages accepted by the relay during the current billing   cycle (used for cost calculation). - **&#x60;cost&#x60;** — estimated cost for the billing cycle based on the base plan price   plus per-email charges. - **&#x60;received&#x60;** / **&#x60;sent&#x60;** — count of messages accepted by the relay /   successfully delivered to the destination MX within the selected &#x60;time&#x60; window. - **&#x60;volume&#x60;** — top-500 breakdown of message counts grouped by source IP (&#x60;ip&#x60;),   destination address (&#x60;to&#x60;), and sender address (&#x60;from&#x60;) within the selected window.   **Time windows** (controlled by the &#x60;time&#x60; parameter): | Value | Window | |-------|--------| | &#x60;1h&#x60; | Last 1 hour (default) | | &#x60;24h&#x60; | Last 24 hours | | &#x60;7d&#x60; | Last 7 days | | &#x60;month&#x60; | Current calendar month (1st to now) | | &#x60;day&#x60; | Today (midnight to now) | | &#x60;billing&#x60; | Current billing cycle (last invoice date to next invoice date) | | &#x60;all&#x60; | All time | 
    # @return [Array<(MailStatsType, Integer, Hash)>] MailStatsType data, response status code and response headers
    def get_stats_with_http_info(time : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: HistoryApi.get_stats ..."}
      end
      allowable_values = ["all", "billing", "month", "7d", "24h", "day", "1h"]
      if @api_client.config.client_side_validation && time && !allowable_values.includes?(time)
        raise ArgumentError.new("invalid value for \"time\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/mail/stats"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["time"] = time.to_s unless time.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["apiKeyAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"HistoryApi.get_stats",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: HistoryApi#get_stats\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return MailStatsType.from_json(data), status_code, headers
    end

    # Displays the mail log
    # Returns a paginated list of every email message accepted by the relay for your mail account(s).  All filter parameters are optional and combinable.  **Pagination** is controlled by `skip` and `limit`.  The response includes a `total` count so clients can determine how many pages exist.  **Date filtering** uses Unix timestamps (`startDate` / `endDate`).  For example, to retrieve emails sent in January 2024: `startDate=1704067200&endDate=1706745599`.  **Delivery status** can be filtered with the `delivered` parameter: `delivered=1` returns only successfully delivered messages; `delivered=0` returns messages still in queue or that failed.  **Address filtering** distinguishes between the SMTP envelope address (`from`, `to`) and message headers (`headerfrom` for the `From:` header, `replyto` for `Reply-To:`). These may differ when a message is sent on behalf of another address.  The `mailid` parameter corresponds to the `id` field in the returned `MailLogEntry` objects, **not** the `_id` field.  It also matches the transaction ID returned in the `text` field of a successful send response from `/mail/send`, `/mail/advsend`, or `/mail/rawsend`.  The `messageId` parameter searches the `Message-ID` email header (case-insensitive substring match). 
    # @return [MailLog]
    def view_mail_log(id : Int64? = nil, origin : String? = nil, mx : String? = nil, from : String? = nil, to : String? = nil, subject : String? = nil, mailid : String? = nil, message_id : String? = nil, replyto : String? = nil, headerfrom : String? = nil, delivered : Int32? = nil, skip : Int32? = nil, limit : Int32? = nil, start_date : Int64? = nil, end_date : Int64? = nil)
      data, _status_code, _headers = view_mail_log_with_http_info(id, origin, mx, from, to, subject, mailid, message_id, replyto, headerfrom, delivered, skip, limit, start_date, end_date)
      data
    end

    # Displays the mail log
    # Returns a paginated list of every email message accepted by the relay for your mail account(s).  All filter parameters are optional and combinable.  **Pagination** is controlled by &#x60;skip&#x60; and &#x60;limit&#x60;.  The response includes a &#x60;total&#x60; count so clients can determine how many pages exist.  **Date filtering** uses Unix timestamps (&#x60;startDate&#x60; / &#x60;endDate&#x60;).  For example, to retrieve emails sent in January 2024: &#x60;startDate&#x3D;1704067200&amp;endDate&#x3D;1706745599&#x60;.  **Delivery status** can be filtered with the &#x60;delivered&#x60; parameter: &#x60;delivered&#x3D;1&#x60; returns only successfully delivered messages; &#x60;delivered&#x3D;0&#x60; returns messages still in queue or that failed.  **Address filtering** distinguishes between the SMTP envelope address (&#x60;from&#x60;, &#x60;to&#x60;) and message headers (&#x60;headerfrom&#x60; for the &#x60;From:&#x60; header, &#x60;replyto&#x60; for &#x60;Reply-To:&#x60;). These may differ when a message is sent on behalf of another address.  The &#x60;mailid&#x60; parameter corresponds to the &#x60;id&#x60; field in the returned &#x60;MailLogEntry&#x60; objects, **not** the &#x60;_id&#x60; field.  It also matches the transaction ID returned in the &#x60;text&#x60; field of a successful send response from &#x60;/mail/send&#x60;, &#x60;/mail/advsend&#x60;, or &#x60;/mail/rawsend&#x60;.  The &#x60;messageId&#x60; parameter searches the &#x60;Message-ID&#x60; email header (case-insensitive substring match). 
    # @return [Array<(MailLog, Integer, Hash)>] MailLog data, response status code and response headers
    def view_mail_log_with_http_info(id : Int64? = nil, origin : String? = nil, mx : String? = nil, from : String? = nil, to : String? = nil, subject : String? = nil, mailid : String? = nil, message_id : String? = nil, replyto : String? = nil, headerfrom : String? = nil, delivered : Int32? = nil, skip : Int32? = nil, limit : Int32? = nil, start_date : Int64? = nil, end_date : Int64? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: HistoryApi.view_mail_log ..."}
      end
      if @api_client.config.client_side_validation && !mailid.nil? && mailid.to_s.size > 19
        raise ArgumentError.new("invalid value for \"mailid\" when calling HistoryApi.view_mail_log, the character length must be smaller than or equal to 19.")
      end

      if @api_client.config.client_side_validation && !mailid.nil? && mailid.to_s.size < 18
        raise ArgumentError.new("invalid value for \"mailid\" when calling HistoryApi.view_mail_log, the character length must be greater than or equal to 18.")
      end

      allowable_values = ["0", "1"]
      if @api_client.config.client_side_validation && delivered && !allowable_values.includes?(delivered)
        raise ArgumentError.new("invalid value for \"delivered\", must be one of #{allowable_values}")
      end
      if @api_client.config.client_side_validation && !skip.nil? && skip < 0
        raise ArgumentError.new("invalid value for \"skip\" when calling HistoryApi.view_mail_log, must be greater than or equal to 0.")
      end

      if @api_client.config.client_side_validation && !limit.nil? && limit > 10000
        raise ArgumentError.new("invalid value for \"limit\" when calling HistoryApi.view_mail_log, must be smaller than or equal to 10000.")
      end

      if @api_client.config.client_side_validation && !limit.nil? && limit < 1
        raise ArgumentError.new("invalid value for \"limit\" when calling HistoryApi.view_mail_log, must be greater than or equal to 1.")
      end

      if @api_client.config.client_side_validation && !start_date.nil? && start_date > 9999999999
        raise ArgumentError.new("invalid value for \"start_date\" when calling HistoryApi.view_mail_log, must be smaller than or equal to 9999999999.")
      end

      if @api_client.config.client_side_validation && !start_date.nil? && start_date < 0
        raise ArgumentError.new("invalid value for \"start_date\" when calling HistoryApi.view_mail_log, must be greater than or equal to 0.")
      end

      if @api_client.config.client_side_validation && !end_date.nil? && end_date > 9999999999
        raise ArgumentError.new("invalid value for \"end_date\" when calling HistoryApi.view_mail_log, must be smaller than or equal to 9999999999.")
      end

      if @api_client.config.client_side_validation && !end_date.nil? && end_date < 0
        raise ArgumentError.new("invalid value for \"end_date\" when calling HistoryApi.view_mail_log, must be greater than or equal to 0.")
      end

      # resource path
      local_var_path = "/mail/log"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["id"] = id.to_s unless id.nil?
      query_params["origin"] = origin.to_s unless origin.nil?
      query_params["mx"] = mx.to_s unless mx.nil?
      query_params["from"] = from.to_s unless from.nil?
      query_params["to"] = to.to_s unless to.nil?
      query_params["subject"] = subject.to_s unless subject.nil?
      query_params["mailid"] = mailid.to_s unless mailid.nil?
      query_params["messageId"] = message_id.to_s unless message_id.nil?
      query_params["replyto"] = replyto.to_s unless replyto.nil?
      query_params["headerfrom"] = headerfrom.to_s unless headerfrom.nil?
      query_params["delivered"] = delivered.to_s unless delivered.nil?
      query_params["skip"] = skip.to_s unless skip.nil?
      query_params["limit"] = limit.to_s unless limit.nil?
      query_params["startDate"] = start_date.to_s unless start_date.nil?
      query_params["endDate"] = end_date.to_s unless end_date.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["apiKeyAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"HistoryApi.view_mail_log",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: HistoryApi#view_mail_log\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return MailLog.from_json(data), status_code, headers
    end
  end
end
