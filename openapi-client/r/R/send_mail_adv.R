#' MailBaby Email Delivery API
#'
#' **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**   # 📌 Overview  This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).   # 🔐 Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site.    We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#'
#' The version of the OpenAPI document: 1.0.0
#' Contact: support@interserver.net
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title SendMailAdv
#' @description SendMailAdv Class
#' @format An \code{R6Class} generator object
#' @field subject  character
#' @field body  character
#' @field from  \link{SendMailAdvFrom}
#' @field to  list( \link{SendMailAdvToInner} )
#' @field replyto  list( \link{SendMailAdvReplytoInner} ) [optional]
#' @field cc  list( \link{SendMailAdvCcInner} ) [optional]
#' @field bcc  list( \link{SendMailAdvBccInner} ) [optional]
#' @field attachments  list( \link{SendMailAdvAttachmentsInner} ) [optional]
#' @field id  integer [optional]
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SendMailAdv <- R6::R6Class(
  "SendMailAdv",
  public = list(
    `subject` = NULL,
    `body` = NULL,
    `from` = NULL,
    `to` = NULL,
    `replyto` = NULL,
    `cc` = NULL,
    `bcc` = NULL,
    `attachments` = NULL,
    `id` = NULL,
    #' Initialize a new SendMailAdv class.
    #'
    #' @description
    #' Initialize a new SendMailAdv class.
    #'
    #' @param subject The subject or title of the email
    #' @param body The main email contents.
    #' @param from from
    #' @param to A list of destionation email addresses to send this to
    #' @param replyto (optional) A list of email addresses that specify where replies to the email should be sent instead of the _from_ address.
    #' @param cc (optional) A list of email addresses to carbon copy this message to.  They are listed on the email and anyone getting the email can see this full list of Contacts who received the email as well.
    #' @param bcc (optional) list of email addresses that should receive copies of the email.  They are hidden on the email and anyone gettitng the email would not see the other people getting the email in this list.
    #' @param attachments (optional) File attachments to include in the email.  The file contents must be base64 encoded!
    #' @param id (optional)  ID of the Mail order within our system to use as the Mail Account.
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `subject`, `body`, `from`, `to`, `replyto` = NULL, `cc` = NULL, `bcc` = NULL, `attachments` = NULL, `id` = NULL, ...
    ) {
      if (!missing(`subject`)) {
        stopifnot(is.character(`subject`), length(`subject`) == 1)
        self$`subject` <- `subject`
      }
      if (!missing(`body`)) {
        stopifnot(is.character(`body`), length(`body`) == 1)
        self$`body` <- `body`
      }
      if (!missing(`from`)) {
        stopifnot(R6::is.R6(`from`))
        self$`from` <- `from`
      }
      if (!missing(`to`)) {
        stopifnot(is.vector(`to`), length(`to`) != 0)
        sapply(`to`, function(x) stopifnot(R6::is.R6(x)))
        self$`to` <- `to`
      }
      if (!is.null(`replyto`)) {
        stopifnot(is.vector(`replyto`), length(`replyto`) != 0)
        sapply(`replyto`, function(x) stopifnot(R6::is.R6(x)))
        self$`replyto` <- `replyto`
      }
      if (!is.null(`cc`)) {
        stopifnot(is.vector(`cc`), length(`cc`) != 0)
        sapply(`cc`, function(x) stopifnot(R6::is.R6(x)))
        self$`cc` <- `cc`
      }
      if (!is.null(`bcc`)) {
        stopifnot(is.vector(`bcc`), length(`bcc`) != 0)
        sapply(`bcc`, function(x) stopifnot(R6::is.R6(x)))
        self$`bcc` <- `bcc`
      }
      if (!is.null(`attachments`)) {
        stopifnot(is.vector(`attachments`), length(`attachments`) != 0)
        sapply(`attachments`, function(x) stopifnot(R6::is.R6(x)))
        self$`attachments` <- `attachments`
      }
      if (!is.null(`id`)) {
        stopifnot(is.numeric(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return SendMailAdv in JSON format
    #' @export
    toJSON = function() {
      SendMailAdvObject <- list()
      if (!is.null(self$`subject`)) {
        SendMailAdvObject[["subject"]] <-
          self$`subject`
      }
      if (!is.null(self$`body`)) {
        SendMailAdvObject[["body"]] <-
          self$`body`
      }
      if (!is.null(self$`from`)) {
        SendMailAdvObject[["from"]] <-
          self$`from`$toJSON()
      }
      if (!is.null(self$`to`)) {
        SendMailAdvObject[["to"]] <-
          lapply(self$`to`, function(x) x$toJSON())
      }
      if (!is.null(self$`replyto`)) {
        SendMailAdvObject[["replyto"]] <-
          lapply(self$`replyto`, function(x) x$toJSON())
      }
      if (!is.null(self$`cc`)) {
        SendMailAdvObject[["cc"]] <-
          lapply(self$`cc`, function(x) x$toJSON())
      }
      if (!is.null(self$`bcc`)) {
        SendMailAdvObject[["bcc"]] <-
          lapply(self$`bcc`, function(x) x$toJSON())
      }
      if (!is.null(self$`attachments`)) {
        SendMailAdvObject[["attachments"]] <-
          lapply(self$`attachments`, function(x) x$toJSON())
      }
      if (!is.null(self$`id`)) {
        SendMailAdvObject[["id"]] <-
          self$`id`
      }

      SendMailAdvObject
    },
    #' Deserialize JSON string into an instance of SendMailAdv
    #'
    #' @description
    #' Deserialize JSON string into an instance of SendMailAdv
    #'
    #' @param input_json the JSON input
    #' @return the instance of SendMailAdv
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`subject`)) {
        self$`subject` <- this_object$`subject`
      }
      if (!is.null(this_object$`body`)) {
        self$`body` <- this_object$`body`
      }
      if (!is.null(this_object$`from`)) {
        from_object <- SendMailAdvFrom$new()
        from_object$fromJSON(jsonlite::toJSON(this_object$from, auto_unbox = TRUE, digits = NA))
        self$`from` <- from_object
      }
      if (!is.null(this_object$`to`)) {
        self$`to` <- ApiClient$new()$deserializeObj(this_object$`to`, "array[SendMailAdvToInner]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`replyto`)) {
        self$`replyto` <- ApiClient$new()$deserializeObj(this_object$`replyto`, "array[SendMailAdvReplytoInner]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`cc`)) {
        self$`cc` <- ApiClient$new()$deserializeObj(this_object$`cc`, "array[SendMailAdvCcInner]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`bcc`)) {
        self$`bcc` <- ApiClient$new()$deserializeObj(this_object$`bcc`, "array[SendMailAdvBccInner]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`attachments`)) {
        self$`attachments` <- ApiClient$new()$deserializeObj(this_object$`attachments`, "array[SendMailAdvAttachmentsInner]", loadNamespace("openapi"))
      }
      if (!is.null(this_object$`id`)) {
        self$`id` <- this_object$`id`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return SendMailAdv in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`subject`)) {
          sprintf(
          '"subject":
            "%s"
                    ',
          self$`subject`
          )
        },
        if (!is.null(self$`body`)) {
          sprintf(
          '"body":
            "%s"
                    ',
          self$`body`
          )
        },
        if (!is.null(self$`from`)) {
          sprintf(
          '"from":
          %s
          ',
          jsonlite::toJSON(self$`from`$toJSON(), auto_unbox=TRUE, digits = NA)
          )
        },
        if (!is.null(self$`to`)) {
          sprintf(
          '"to":
          [%s]
',
          paste(sapply(self$`to`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`replyto`)) {
          sprintf(
          '"replyto":
          [%s]
',
          paste(sapply(self$`replyto`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`cc`)) {
          sprintf(
          '"cc":
          [%s]
',
          paste(sapply(self$`cc`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`bcc`)) {
          sprintf(
          '"bcc":
          [%s]
',
          paste(sapply(self$`bcc`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`attachments`)) {
          sprintf(
          '"attachments":
          [%s]
',
          paste(sapply(self$`attachments`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
          '"id":
            %d
                    ',
          self$`id`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of SendMailAdv
    #'
    #' @description
    #' Deserialize JSON string into an instance of SendMailAdv
    #'
    #' @param input_json the JSON input
    #' @return the instance of SendMailAdv
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`subject` <- this_object$`subject`
      self$`body` <- this_object$`body`
      self$`from` <- SendMailAdvFrom$new()$fromJSON(jsonlite::toJSON(this_object$from, auto_unbox = TRUE, digits = NA))
      self$`to` <- ApiClient$new()$deserializeObj(this_object$`to`, "array[SendMailAdvToInner]", loadNamespace("openapi"))
      self$`replyto` <- ApiClient$new()$deserializeObj(this_object$`replyto`, "array[SendMailAdvReplytoInner]", loadNamespace("openapi"))
      self$`cc` <- ApiClient$new()$deserializeObj(this_object$`cc`, "array[SendMailAdvCcInner]", loadNamespace("openapi"))
      self$`bcc` <- ApiClient$new()$deserializeObj(this_object$`bcc`, "array[SendMailAdvBccInner]", loadNamespace("openapi"))
      self$`attachments` <- ApiClient$new()$deserializeObj(this_object$`attachments`, "array[SendMailAdvAttachmentsInner]", loadNamespace("openapi"))
      self$`id` <- this_object$`id`
      self
    },
    #' Validate JSON input with respect to SendMailAdv
    #'
    #' @description
    #' Validate JSON input with respect to SendMailAdv and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `subject`
      if (!is.null(input_json$`subject`)) {
        stopifnot(is.character(input_json$`subject`), length(input_json$`subject`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for SendMailAdv: the required field `subject` is missing."))
      }
      # check the required field `body`
      if (!is.null(input_json$`body`)) {
        stopifnot(is.character(input_json$`body`), length(input_json$`body`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for SendMailAdv: the required field `body` is missing."))
      }
      # check the required field `from`
      if (!is.null(input_json$`from`)) {
        stopifnot(R6::is.R6(input_json$`from`))
      } else {
        stop(paste("The JSON input `", input, "` is invalid for SendMailAdv: the required field `from` is missing."))
      }
      # check the required field `to`
      if (!is.null(input_json$`to`)) {
        stopifnot(is.vector(input_json$`to`), length(json_input$`to`) != 0)
        tmp <- sapply(input_json$`to`, function(x) stopifnot(R6::is.R6(x)))
      } else {
        stop(paste("The JSON input `", input, "` is invalid for SendMailAdv: the required field `to` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of SendMailAdv
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

