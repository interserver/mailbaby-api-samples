#' MailBaby Email Delivery API
#'
#' **Send emails fast and with confidence through our easy to use [REST](https://en.wikipedia.org/wiki/Representational_state_transfer) API interface.**   # 📌 Overview  This is the API interface to the [Mail Baby](https//mail.baby/) Mail services provided by [InterServer](https://www.interserver.net). To use this service you must have an account with us at [my.interserver.net](https://my.interserver.net).   # 🔐 Authentication  In order to use most of the API calls you must pass credentials from the [my.interserver.net](https://my.interserver.net/) site.    We support several different authentication methods but the preferred method is to use the **API Key** which you can get from the [Account Security](https://my.interserver.net/account_security) page. 
#'
#' The version of the OpenAPI document: 1.0.0
#' Contact: support@interserver.net
#' Generated by: https://openapi-generator.tech
#'

#' @docType class
#' @title SendMailAdvCcInner
#' @description SendMailAdvCcInner Class
#' @format An \code{R6Class} generator object
#' @field email  character
#' @field name  character [optional]
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SendMailAdvCcInner <- R6::R6Class(
  "SendMailAdvCcInner",
  public = list(
    `email` = NULL,
    `name` = NULL,
    #' Initialize a new SendMailAdvCcInner class.
    #'
    #' @description
    #' Initialize a new SendMailAdvCcInner class.
    #'
    #' @param email The email address
    #' @param name (optional) Name to use for the CC contact.
    #' @param ... Other optional arguments.
    #' @export
    initialize = function(
        `email`, `name` = NULL, ...
    ) {
      if (!missing(`email`)) {
        stopifnot(is.character(`email`), length(`email`) == 1)
        self$`email` <- `email`
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return SendMailAdvCcInner in JSON format
    #' @export
    toJSON = function() {
      SendMailAdvCcInnerObject <- list()
      if (!is.null(self$`email`)) {
        SendMailAdvCcInnerObject[["email"]] <-
          self$`email`
      }
      if (!is.null(self$`name`)) {
        SendMailAdvCcInnerObject[["name"]] <-
          self$`name`
      }

      SendMailAdvCcInnerObject
    },
    #' Deserialize JSON string into an instance of SendMailAdvCcInner
    #'
    #' @description
    #' Deserialize JSON string into an instance of SendMailAdvCcInner
    #'
    #' @param input_json the JSON input
    #' @return the instance of SendMailAdvCcInner
    #' @export
    fromJSON = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      if (!is.null(this_object$`email`)) {
        self$`email` <- this_object$`email`
      }
      if (!is.null(this_object$`name`)) {
        self$`name` <- this_object$`name`
      }
      self
    },
    #' To JSON string
    #'
    #' @description
    #' To JSON String
    #'
    #' @return SendMailAdvCcInner in JSON format
    #' @export
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`email`)) {
          sprintf(
          '"email":
            "%s"
                    ',
          self$`email`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
          '"name":
            "%s"
                    ',
          self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      as.character(jsonlite::minify(paste("{", jsoncontent, "}", sep = "")))
    },
    #' Deserialize JSON string into an instance of SendMailAdvCcInner
    #'
    #' @description
    #' Deserialize JSON string into an instance of SendMailAdvCcInner
    #'
    #' @param input_json the JSON input
    #' @return the instance of SendMailAdvCcInner
    #' @export
    fromJSONString = function(input_json) {
      this_object <- jsonlite::fromJSON(input_json)
      self$`email` <- this_object$`email`
      self$`name` <- this_object$`name`
      self
    },
    #' Validate JSON input with respect to SendMailAdvCcInner
    #'
    #' @description
    #' Validate JSON input with respect to SendMailAdvCcInner and throw an exception if invalid
    #'
    #' @param input the JSON input
    #' @export
    validateJSON = function(input) {
      input_json <- jsonlite::fromJSON(input)
      # check the required field `email`
      if (!is.null(input_json$`email`)) {
        stopifnot(is.character(input_json$`email`), length(input_json$`email`) == 1)
      } else {
        stop(paste("The JSON input `", input, "` is invalid for SendMailAdvCcInner: the required field `email` is missing."))
      }
    },
    #' To string (JSON format)
    #'
    #' @description
    #' To string (JSON format)
    #'
    #' @return String representation of SendMailAdvCcInner
    #' @export
    toString = function() {
      self$toJSONString()
    }
  )
)

